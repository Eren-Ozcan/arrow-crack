import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yilkgames.arrowcrack",
  appName: "Arrow Crack",
  webDir: "dist",
  android: {
    backgroundColor: "#12131A",
  },
};

export default config;
